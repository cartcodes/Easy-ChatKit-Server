# Open AI Api Key - Must be from the same org & project as your Agent Builder workflow
OPENAI_API_KEY=sk-proj-your-key-here

# Workflow ID from Agent Builder
CHATKIT_WORKFLOW_ID=wf_your-workflow-id

# Register your server's domain on the domain allowlist page
# Get this from OpenAI Platform after adding your domains
DOMAIN_KEY=pk_your-domain-key

# Server Port
PORT=3000

# Base URL for the server (used in generated code snippets)
BASE_URL=http://localhost:3000

# Environment (development or production)
# Set to 'production' on deployed server to hide the customization UI
NODE_ENV=development
